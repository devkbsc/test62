{% extends 'base.html.twig' %}

{% block title %}Edit Attribution{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1>Edit Attribution</h1>

    {{ include('attribution/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('app_attribution_index') }}">back to list</a>

    {{ include('attribution/_delete_form.html.twig') }}
    </div>
{% endblock %}
